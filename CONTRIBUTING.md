# Contributing

Use Python 3.13+, Poetry, typed public APIs, dependency injection, and deterministic tests. Run `make check` before submitting changes. Keep provider and persistence details behind protocols, and do not add network-dependent tests.
